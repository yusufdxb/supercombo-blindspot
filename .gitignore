.venv/
__pycache__/
*.pyc
.pytest_cache/
logs/
*.mp4
*.csv
.DS_Store
models/*.onnx
# Reproducible inputs/outputs (HEVC, rlogs, survey/scenario PNGs, capture .npy).
# Re-fetched from the Azure CI bucket / regenerated by the scripts.
data/
requirements.txt.py311.bak
# report/figures/*.png ARE tracked (deliverable) — only data/ images are ignored.
