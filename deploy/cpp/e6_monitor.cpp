/**
 * e6_monitor.cpp
 *
 * This translation unit is intentionally empty: E6Monitor is fully
 * defined in the header (e6_monitor.hpp).  This file exists so the
 * Makefile can build a linkable object for projects that prefer
 * separate-compilation builds.
 */

#include "e6_monitor.hpp"
